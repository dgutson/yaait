__pycache__/
*.pyc
.venv/
.DS_Store

# Daniel's source deck — cited textually in skills/design/references/smells.md §11.
# 15 MB and already zip-compressed, so it bloats every clone and every plugin install.
*.pptx
HANDOFF.md
